<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cluster HA - Infrastructure DevOps</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #fff;
        }
        
        .container {
            text-align: center;
            padding: 40px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            backdrop-filter: blur(10px);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            max-width: 800px;
            margin: 20px;
        }
        
        .logo {
            font-size: 80px;
            margin-bottom: 20px;
        }
        
        h1 {
            font-size: 2.5rem;
            margin-bottom: 10px;
            background: linear-gradient(90deg, #00d9ff, #00ff88);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .subtitle {
            font-size: 1.2rem;
            color: #aaa;
            margin-bottom: 30px;
        }
        
        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }
        
        .info-card {
            background: rgba(255, 255, 255, 0.05);
            padding: 20px;
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .info-card h3 {
            color: #00d9ff;
            margin-bottom: 10px;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .info-card p {
            font-size: 1.3rem;
            font-weight: bold;
        }
        
        .status {
            display: inline-block;
            padding: 8px 20px;
            background: linear-gradient(90deg, #00ff88, #00d9ff);
            color: #1a1a2e;
            border-radius: 25px;
            font-weight: bold;
            margin: 20px 0;
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }
        
        .services {
            margin-top: 30px;
            text-align: left;
        }
        
        .services h2 {
            color: #00d9ff;
            margin-bottom: 15px;
            font-size: 1.2rem;
        }
        
        .service-list {
            list-style: none;
        }
        
        .service-list li {
            padding: 10px 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            display: flex;
            align-items: center;
        }
        
        .service-list li:before {
            content: '✓';
            color: #00ff88;
            margin-right: 15px;
            font-weight: bold;
        }
        
        .footer {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: #666;
            font-size: 0.9rem;
        }
        
        .vip {
            color: #00ff88;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">🖥️</div>
        <h1>Cluster Haute Disponibilité</h1>
        <p class="subtitle">Infrastructure DevOps - Mega TP</p>
        
        <div class="status">● CLUSTER ACTIF</div>
        
        <div class="info-grid">
            <div class="info-card">
                <h3>Serveur Actif</h3>
                <p>{{ ansible_hostname }}</p>
            </div>
            <div class="info-card">
                <h3>Adresse IP</h3>
                <p>{{ ansible_host }}</p>
            </div>
            <div class="info-card">
                <h3>VIP Cluster</h3>
                <p class="vip">{{ vip_address }}</p>
            </div>
            <div class="info-card">
                <h3>Système</h3>
                <p>{{ ansible_distribution }} {{ ansible_distribution_major_version }}</p>
            </div>
        </div>
        
        <div class="services">
            <h2>📦 Services Haute Disponibilité</h2>
            <ul class="service-list">
                <li>Nginx - Serveur Web (Active/Passive)</li>
                <li>Samba - Partage de fichiers SMB</li>
                <li>Pacemaker - Gestionnaire de cluster</li>
                <li>Corosync - Communication cluster</li>
                <li>Zabbix Agent - Supervision</li>
            </ul>
        </div>
        
        <div class="footer">
            <p>🔄 Dernière mise à jour: {{ ansible_date_time.iso8601 }}</p>
            <p>Cluster: {{ cluster_name }} | Domaine: {{ domain_name }}</p>
        </div>
    </div>
</body>
</html>
